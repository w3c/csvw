{
  "@context": "http://www.w3.org/ns/csvw",
  "rdfs:comment": "Numeric dataype with object format.",
  "rdfs:label": "number format (valid combinations)",
  "url": "test158.csv",
  "tableSchema": {
    "columns": [
      {"titles": "integer", "datatype": {"base": "integer", "format": {"pattern": "^\\d+$"}}},
      {"titles": "decimal", "datatype": {"base": "decimal", "format": {"pattern": "^\\d+\\.\\d$"}}},
      {"titles": "double", "datatype": {"base": "double", "format": {"pattern": "^\\d+\\.\\d+[eE]\\d+$"}}},
      {"titles": "decimal1", "datatype": {"base": "decimal", "format": {"decimalChar": ",", "groupChar": "_"}}}
    ]
  }
}